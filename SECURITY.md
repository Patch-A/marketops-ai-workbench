# Security Policy

## 当前状态

项目尚处于规格和原型阶段，不应处理真实敏感客户资料。公开演示只能使用虚构或充分脱敏的数据。

## 设计要求

- 工作空间、客户和项目必须有明确的数据边界。
- 原始文件、解析文本、向量、缓存和备份使用同一删除策略。
- 模型调用前必须展示将发送的数据范围，并支持按项目关闭外部模型。
- API 密钥只保存在服务端密钥存储，不写入浏览器、本地日志或提示词。
- 所有正式外发、权限变更和知识提升操作必须写入审计日志。
- 连接器使用最小权限；飞书、企微和邮箱连接器可以单独禁用和撤销。
- 个人微信的非官方自动化不进入首版支持范围。

## 报告问题

公开仓库建立后，请通过 GitHub Security Advisory 私下报告漏洞，不要在公开 Issue 中附加真实客户数据、访问令牌或可利用细节。
